echo What is in this directory?
          ls -a
          echo
          echo Is java installed?
          java --version
          echo
          echo Is git installed?
          git --version
          echo What about build tool?
          mvn --version
          gradle --version
